namespace GbxTest
{
    public enum BlockMode
    {
        START,
        MULTILAP,
        FINISH,
        CHECKPOINT
    }

    public enum EqMode
    {
        LESS_THAN,
        LESS_OR_EQUAL,
        EQUAL_TO,
        GREATER_THAN,
        GREATER_OR_EQUAL
    }
}
