namespace GbxTest
{
    public enum BlockMode
    {
        START = 0,
        FINISH = 1
    }

    public enum EqMode
    {
        LESS_THAN = 0,
        LESS_OR_EQUAL = 1,
        EQUAL_TO = 2,
        GREATER_THAN = 3,
        GREATER_OR_EQUAL = 4
    }
}
